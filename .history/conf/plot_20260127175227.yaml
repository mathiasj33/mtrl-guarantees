defaults:
  - hydra: experiment
  - _self_

name: plot
run: main

env:
  name: cheetah

results:
  dir: ../${env.name}/eval/${run}

plot:
  bounds: [bernstein]
  num_tasks: [500]
  num_episodes: [100, 500, 1000]
